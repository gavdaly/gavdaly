---
import type { Project } from "@/data/projects";

type Props = Project;

const { title, technology, status, slug } = Astro.props;

const formatTechs = (values: string[]) =>
  values.toString().replaceAll(",", " • ");
---

<a
  class="group w-full cursor-pointer justify-between gap-2 rounded-2xl border border-blue-800 p-4 transition-all duration-300 hover:-translate-y-2 md:flex-row md:items-center"
  href={`/projects/${slug}`}
>
  <div class="flex flex-wrap gap-2 justify-between mb-2">
    <h2 class="font-bold text-lg">{title}</h2>

    <span class="w-fit rounded-lg border border-blue-800 px-4 py-1"
      >{status}</span
    >
  </div>
  <div class="flex flex-col gap-2 md:flex-row md:items-center md:gap-4">
    <span class="truncate font-thin text-sm">{formatTechs(technology)}</span>
  </div>
</a>
