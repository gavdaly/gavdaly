# Local environment example
# Copy to .env and fill in values for local testing.

# Discord webhook for contact form notifications (Cloudflare binding in prod)
DISCORD_WEBHOOK="https://discord.com/api/webhooks/xxxx/xxxx"

# Cloudflare Turnstile secret (server-side validation)
TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"

# Cloudflare KV and D1 are configured as bindings in Pages/Workers.
# These entries are placeholders for local tools only.
# KV: referral shortlinks
GAV_DALY_REFERRAL="kv-namespace-id-or-name"

# D1: database for forms/redirect logs
DB="d1-database-id-or-name"

